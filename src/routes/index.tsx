import AppRoutes from "@routes/app.routes"; // Importing the main application routes component

// Function component to render the application routes
function Routes() {
  return <AppRoutes />; // Rendering the AppRoutes component
}

export default Routes; // Exporting the component to be used elsewhere in the application
